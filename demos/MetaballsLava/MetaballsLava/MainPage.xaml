<Page x:Class="MetaballsLava.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MetaballsLava"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <!-- Grid with a (transparent) Background is reliably hit-testable, so pointer
       events fire here and are forwarded to the canvas. -->
  <Grid x:Name="Root" Background="Transparent"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased"
        PointerWheelChanged="OnPointerWheel">

    <local:DemoCanvas x:Name="Canvas" />

    <!-- Minimal Fluent overlay: a Reset button tucked into the top-right corner. -->
    <Button x:Name="ResetButton"
            Content="Reset Lamp"
            Click="OnReset"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="0,20,24,0"
            Padding="14,8"
            Background="#33FF7A2B"
            BorderBrush="#66FFB347"
            BorderThickness="1"
            Foreground="White"
            CornerRadius="8" />

  </Grid>
</Page>
