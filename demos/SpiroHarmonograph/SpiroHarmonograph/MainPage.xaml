<Page x:Class="SpiroHarmonograph.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SpiroHarmonograph"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <!-- Grid with a (transparent) Background is reliably hit-testable, so pointer
       events fire here and are forwarded to the canvas. -->
  <Grid x:Name="Root" Background="Transparent"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased"
        PointerWheelChanged="OnPointerWheel">

    <local:DemoCanvas x:Name="Canvas" />

    <!-- Fluent controls overlay, tucked into the top-right corner. -->
    <Border HorizontalAlignment="Right" VerticalAlignment="Top"
            Margin="0,20,20,0" Padding="14,12"
            CornerRadius="12"
            Background="#66101428"
            BorderBrush="#33FFFFFF" BorderThickness="1">
      <StackPanel Spacing="10" MinWidth="190">
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Content="Randomize" Click="OnRandomize" />
          <Button Content="Clear" Click="OnClear" />
        </StackPanel>
        <TextBlock Text="Damping" FontSize="12" Opacity="0.8" />
        <Slider x:Name="DampingSlider"
                Minimum="0.3" Maximum="2.0" StepFrequency="0.05"
                Value="1.0"
                ValueChanged="OnDampingChanged" />
      </StackPanel>
    </Border>

  </Grid>
</Page>
