<Page x:Class="SkiaFlap.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SkiaFlap"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <!-- IsTabStop + programmatic Focus makes the Grid receive KeyDown/KeyUp.
       Transparent Background makes it hit-testable for pointer events. -->
  <Grid x:Name="Root" Background="Transparent" IsTabStop="True"
        Loaded="OnLoaded"
        KeyDown="OnKeyDown"
        KeyUp="OnKeyUp"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased"
        PointerWheelChanged="OnPointerWheel">

    <local:DemoCanvas x:Name="Canvas" />

    <!-- Minimal Fluent overlay: a Restart button. The in-canvas HUD draws the
         score, controls hint and game-over panel. -->
    <Button x:Name="RestartButton"
            Content="Restart"
            Click="OnRestart"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Margin="16,16,0,0" />

  </Grid>
</Page>
