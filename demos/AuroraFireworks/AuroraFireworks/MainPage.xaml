<Page x:Class="AuroraFireworks.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:AuroraFireworks"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <!-- Grid with a (transparent) Background is reliably hit-testable, so pointer
       events fire here and are forwarded to the canvas. -->
  <Grid x:Name="Root" Background="Transparent"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased"
        PointerWheelChanged="OnPointerWheel">

    <local:DemoCanvas x:Name="Canvas" />

    <!-- Optional Fluent controls overlay; agents may add Buttons/Sliders here.
         Keep it minimal and out of the way (e.g. top-left or bottom). -->

  </Grid>
</Page>
