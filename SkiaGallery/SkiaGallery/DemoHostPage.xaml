<Page x:Class="SkiaGallery.DemoHostPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:core="using:SkiaGallery.Core"
      Background="#06070D">

  <Grid x:Name="Root" Background="Transparent" IsTabStop="True"
        Loaded="OnLoaded"
        KeyDown="OnKeyDown" KeyUp="OnKeyUp"
        PointerPressed="OnPointerPressed" PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased" PointerWheelChanged="OnPointerWheel">

    <core:GalleryCanvas x:Name="Canvas" />

    <Border VerticalAlignment="Top" HorizontalAlignment="Stretch"
            Background="#B3000000" Padding="14,9">
      <StackPanel Orientation="Horizontal" Spacing="14" VerticalAlignment="Center">
        <Button Content="&#8592; Back" Click="OnBack" />
        <StackPanel VerticalAlignment="Center">
          <TextBlock x:Name="TitleText" Foreground="#EEF1FB" FontWeight="SemiBold" FontSize="15" />
          <TextBlock x:Name="ControlsText" Foreground="#9AA3C0" FontSize="12" TextWrapping="NoWrap" />
        </StackPanel>
      </StackPanel>
    </Border>
  </Grid>
</Page>
